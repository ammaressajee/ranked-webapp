rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /early_signups/{docId} {
      allow create: if true; // allow anyone to submit emails
      allow read, update, delete: if false;
    }
  }
}
